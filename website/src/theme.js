import { createTheme } from "@mui/system";

export const light = createTheme({
    palette: {
        primary: {
            main: red[500],
        },      
    }
})


export const dark = createTheme({
    palette: {
        primary: {
            main: red[500],
        },      
    }
})